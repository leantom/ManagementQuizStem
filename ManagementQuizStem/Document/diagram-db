graph TD
  subgraph Firestore
    S["Subjects/{subjectId}
- name
- short_name
- description
- trending
- icon_url
- color_hex
- topicIds[]"]

    T["Topics/{topicId}
- id
- name
- category
- description
- iconURL
- educationLevel
- trending"]

    Q["questions/{questionId}
- source
- externalId
- isVerified
- scientificDomain
- didYouKnow
- questionText
- options[]
- correctAnswer
- topicID
- difficulty
- cognitiveSkills[]
- eloRating
- hints[]
- explanation
- realWorldContext
- questionId = sha256(questionText)"]

    U["users/{userId}
- mentalRadar{}
- streak{}
- unlocks[]
- preferences{}"]

    LP["learningPaths/{pathId}
- title
- description
- difficulty
- steps[]
- createdAt
- updatedAt"]

    DC["dailyChallenges/{challengeId}
- date
- questionId ref
- globalStats{}"]

    C["challenges/{challengeId}
- type
- title
- description
- startDate
- remainTime
- endDate
- difficultyLevel
- questions[]
- rewards[]
- isActive
- createdAt
- updatedAt"]

    B["badges/{badgeId}
- title
- description
- icon
- criteria
- createdAt
- updatedAt"]

    SAT["satExamQuestions/{questionId}
- domain
- difficulty
- createdAt
- SAT payload fields"]

    LQ["Questions/{questionId}
- legacy root collection
- keep read-only during migration"]

    NTQ["Topics/{topicId}/Questions
- legacy nested subcollection"]

    NTQL["Topics/{topicId}/questions
- lowercase mirror path
- only used if mirror flag is enabled"]
  end

  subgraph Embedded_Documents
    MR["mentalRadar
- logic: Int
- estimation: Int
- data_literacy: Int
- cognitive_domain: Int"]

    ST["streak
- currentStreak
- longestStreak
- lastActiveDate"]

    PREF["preferences
- goals[]
- focusSkills[]
- preferredDifficultyELO"]

    STEP["LearningPathStep
- title
- questionIds[]
- cognitiveSkills[]
- targetELO"]

    GS2["DailyChallengeGlobalStats
- totalAttempts
- correctAttempts
- optionDistribution{}"]

    BC["BadgeCriteria
- action
- topic
- accuracy
- question
- timeLimit
- streak"]

    TW["TimeWindow
- startTime
- endTime"]

    RW["Reward
- type
- value
- description"]
  end

  subgraph Cloud_Storage
    GS["Storage
/icons/{id}.jpg"]
  end

  S -->|"topicIds[] declared relation"| T
  T -.->|"UI also matches Topic.name == Subject.name"| S
  Q -->|"topicID"| T
  C -->|"questions[] stores questionIds"| Q
  LP -->|"steps[].questionIds[]"| Q
  DC -->|"questionId reference"| Q
  U -->|"mentalRadar"| MR
  U -->|"streak"| ST
  U -->|"preferences"| PREF
  U -->|"unlocks[] can contain pathId or badgeId"| LP
  U -->|"unlocks[] can contain pathId or badgeId"| B
  LP -->|"steps[]"| STEP
  DC -->|"globalStats"| GS2
  C -->|"rewards[]"| RW
  B -->|"criteria"| BC
  BC -->|"timeWindow"| TW
  S -->|"icon_url"| GS
  T -->|"iconURL"| GS
  LQ -.->|"migrate/copy into"| Q
  T -.->|"legacy write path"| NTQ
  T -.->|"optional mirror path"| NTQL
